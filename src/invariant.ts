/**
 * Invariant companion for dsh-user-question-nav.
 *
 * No runtime invariants: this is a pure client-side UI plugin with no
 * host-side data, event relationships, or service registrations to verify.
 * The client half owns its own DOM cleanup through the cordis effect
 * lifecycle.
 */
export const name = 'dsh-user-question-nav'